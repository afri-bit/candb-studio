/** The kind of CAN database object an attribute or definition applies to. */
export enum ObjectType {
    Network = 'network',
    Node = 'node',
    Message = 'message',
    Signal = 'signal',
    EnvironmentVariable = 'environment_variable',
}
